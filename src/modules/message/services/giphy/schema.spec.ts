import { it } from 'vitest';

it('parses data to ParsedGif object', async () => {
  // Arrange
  // Act
  // Assert
});
it('returns null if response object does not contain data property', async () => {
  // Arrange
  // Act
  // Assert
});
