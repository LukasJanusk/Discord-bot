import { it } from 'vitest';
// throws and error if it fails to acces gif
// successfully returns gif from api
// uses fallback gif if failed to access api

it('returns gif url from API', async () => {
  // Arrange
  // Act
  // Assert
});

it('throws an error if it fails to access API', async () => {
  // Arrange
  // Act
  // Assert
});
